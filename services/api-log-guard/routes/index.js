/**
 * API Routes Index
 * Additional route modules can be imported and registered here
 */

export default function registerRoutes(app, db) {
  // Health check is already in server.js
  // Add additional route modules here as the API grows
  
  // Example:
  // import logsRoutes from './logs.js';
  // import alertsRoutes from './alerts.js';
  // import statsRoutes from './stats.js';
  //
  // logsRoutes(app, db);
  // alertsRoutes(app, db);
  // statsRoutes(app, db);
}
